<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.book.mapper.BorrowRecordMapper">
    
    <resultMap type="com.ruoyi.book.domain.BorrowRecord" id="BorrowRecordResult">
        <result property="recordId"    column="record_id"    />
        <result property="userId"    column="user_id"    />
        <result property="userName"    column="user_name"    />
        <result property="borrowTime"    column="borrow_time"    />
        <result property="planReturnTime"    column="plan_return_time"    />
        <result property="returnTime"    column="return_time"    />
        <result property="status"    column="status"    />
        <result property="bookId"    column="book_id"    />
        <result property="bookName"    column="book_name"    />
        <result property="author"    column="author"    />
        <result property="isbn"    column="isbn"    />
        <result property="category"    column="category"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectBorrowRecordVo">
        select r.record_id, r.user_id, r.user_name, r.borrow_time, r.plan_return_time, r.return_time, r.status, 
               i.book_id, i.book_name, b.author, b.isbn, b.category, 
               r.create_time, r.update_time 
        from borrow_record r
        left join borrow_item i on r.record_id = i.record_id
        left join book b on i.book_id = b.book_id
    </sql>

    <select id="selectBorrowRecordList" parameterType="com.ruoyi.book.domain.BorrowRecord" resultMap="BorrowRecordResult">
        <include refid="selectBorrowRecordVo"/>
        <where>  
            <if test="userId != null "> and r.user_id = #{userId}</if>
            <if test="userName != null  and userName != ''"> and r.user_name like concat('%', #{userName}, '%')</if>
            <if test="borrowTime != null "> and r.borrow_time = #{borrowTime}</if>
            <if test="planReturnTime != null "> and r.plan_return_time = #{planReturnTime}</if>
            <if test="returnTime != null "> and r.return_time = #{returnTime}</if>
            <if test="status != null  and status != ''"> and r.status = #{status}</if>
        </where>
    </select>
    
    <select id="selectBorrowRecordByRecordId" parameterType="Long" resultMap="BorrowRecordResult">
        <include refid="selectBorrowRecordVo"/>
        where r.record_id = #{recordId}
    </select>

    <insert id="insertBorrowRecord" parameterType="com.ruoyi.book.domain.BorrowRecord" useGeneratedKeys="true" keyProperty="recordId">
        insert into borrow_record
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">user_id,</if>
            <if test="userName != null and userName != ''">user_name,</if>
            <if test="borrowTime != null">borrow_time,</if>
            <if test="planReturnTime != null">plan_return_time,</if>
            <if test="returnTime != null">return_time,</if>
            <if test="status != null">status,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateTime != null">update_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userId != null">#{userId},</if>
            <if test="userName != null and userName != ''">#{userName},</if>
            <if test="borrowTime != null">#{borrowTime},</if>
            <if test="planReturnTime != null">#{planReturnTime},</if>
            <if test="returnTime != null">#{returnTime},</if>
            <if test="status != null">#{status},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateTime != null">#{updateTime},</if>
         </trim>
    </insert>

    <update id="updateBorrowRecord" parameterType="com.ruoyi.book.domain.BorrowRecord">
        update borrow_record
        <trim prefix="SET" suffixOverrides=",">
            <if test="userId != null">user_id = #{userId},</if>
            <if test="userName != null and userName != ''">user_name = #{userName},</if>
            <if test="borrowTime != null">borrow_time = #{borrowTime},</if>
            <if test="planReturnTime != null">plan_return_time = #{planReturnTime},</if>
            <if test="returnTime != null">return_time = #{returnTime},</if>
            <if test="status != null">status = #{status},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where record_id = #{recordId}
    </update>

    <delete id="deleteBorrowRecordByRecordId" parameterType="Long">
        delete from borrow_record where record_id = #{recordId}
    </delete>

    <delete id="deleteBorrowRecordByRecordIds" parameterType="String">
        delete from borrow_record where record_id in 
        <foreach item="recordId" collection="array" open="(" separator="," close=")">
            #{recordId}
        </foreach>
    </delete>
</mapper>